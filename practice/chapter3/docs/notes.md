# Week 3 Notes

MCP는 에이전트가 외부 도구를 호출하는 표준 인터페이스를 제공한다.
도구를 연결하는 것만으로는 작업 품질이 자동으로 좋아지지 않는다.
실무에서는 skill, instruction, hook, memory를 함께 설계해야 품질이 안정된다.
plugin은 MCP나 skill과 경쟁 개념이 아니라 배포 단위다.
출력 파일 경로, 검증 순서, 실패 기록 방식이 없으면 결과 재현성이 떨어진다.
