# Week 09-13 Integrated Practice

9~13주차 실습과 과제는 이 폴더 하나에서 이어서 진행한다.

- 저장소 루트의 `.venv`를 공용 가상환경으로 사용한다.
- 주차별 `practice/chapter*/code` 폴더를 새로 만들지 않는다.
- 실습 코드와 실행 파일은 이 폴더에 둔다.
- 보조 문서는 `docs/`, 입력 자료는 `data/input/`, 결과물은 `data/output/`에 둔다.

```bash
# 저장소 루트에서
python3 -m venv .venv
source .venv/bin/activate
cd multi-agent
```
